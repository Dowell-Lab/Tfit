Function parameteters in constructors are named v_paramname to 
distinguish from the names of the components of the objects.

Global variables are named g_paramname.

Every method with logic should have Unit Tests in /test/src
