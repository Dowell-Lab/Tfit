First I changed all of the print statments to have braces

Functions to look at:
Display__iterates**Special/non-crucial
make_test_file
node
model**
model across**
unstranded_model**
parse_argv(main is now changed to handle both hardcoded and arguments)-> connect with usage part of readme
test_new_I_fit **
template_window_matching**

(**) means that tests need to be run to ensure there is not value difference(which is last step in transfer)

Now I have two test files, one in python_src and the other in python3_src, so these two files can test
the files in each environment respectively. Right now I just pipe outputs to the transition docs folder. Still need
to get a good way to compare the two different outputs. As of now I will push foreward in comparing the two versions.
Need to find the large files on fiji so that I can run the two tests to see the difference in versions.
When trying to find fstich training files I could not find any to run fstitch
and then could not get those files to run bug python3 test

My method for testing whether or not th file is used in the core of tfit is:
I do a file wide search for the title of the program.
If the file is used in another program than it would be imported at the top of the other file.
Then I can see what functions are being used.

Questions:
Have they tested the difference in resuts between the two types of code(C++ and Python). I feel like there must be some difference
Answer: No, but I can start on doing some of that.

In model.py:
This error is given for python3: matplotlib does not support generators as input
fixed in python2

simulate.py:
uppon review there are changes from run to run, however the difference between py3-py2 doesnt seem to have a big difference
Could possibly write a script to check the varation between the two

main.py/load.py:
seems good for transition, however when I get tsv files, there is nothing writen to them(in both 2 and 3).(Problem with fstitch files)
also, only hardcode function works. ill try to get the run function working
I may add an option for the preset hardcoded versions-> if there are no arguments the hardcoded version is run.

find_regions_of_intrest.py
just had to change around some print statement brackets

make_test_file:
the only function to work is run2. makes, bedgraph files

make_interval_files:
from a python3 standpoint this looks good

make_noise_region_file:
no problems in python3

display__iterates:
found a pretty big bug: in the run function the list G never gets filled

nodes:
problems with list  indices

unstranded model:
Creates an insane amount of figures. Also just prints them. Changed it to save them in folder "unstranded_model_figures"
There is an error when it starts:"The 'normed' kwarg is deprecated, and has been replaced by the 'density' kwarg."
This is just a python3 error. Now I need to check if this effects the values.
Between runs there is variance as well (due to the simulated natue of the run)

check moment estimators:
looks good
also not used anywhere else in the code

bootrapping:
looks good
Does not seem to be used anywhere else

template window matching:
when it calls simulate, there is an error(in py27)
I had to input correct bounds from example files in the main, but error in py27 went away(kinda)
get the same output in py3, however there is still an index out of range error.
simiare to the same error from model.py(although it happens in both 2 and 3)

test_new_i_fit:
there seems to be a problem with the function, however from a transition standpoint it looks good.
The problem is that some of the math gets hung up with zeros/floats. Not always though.

Model Across:
When I first got to it this file seemed unfinished, with wierd errors, like missing imports and declarations
then I had to create a main function so that I could test it, and since it calls the same function in model that is broken 
this too does not work.

find_regions_of_interest:
It does not does not seem that this file is used at all
Although the window search function seems useful

make_noise_region_file:
this seems to not work well with bed files. I 
Annotated my thoughts on line 13

make interval file:
I have not tested this yet





Notes on Generators:
Many Standard Library functions that return lists in Python 2 have been
modified to return generators in Python 3 because generators require fewer resources.
